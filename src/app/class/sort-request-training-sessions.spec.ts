import { SortRequestTrainingSessions } from './sort-request-training-sessions';

describe('SortRequestTrainingSessions', () => {
  it('should create an instance', () => {
    expect(new SortRequestTrainingSessions()).toBeTruthy();
  });
});
