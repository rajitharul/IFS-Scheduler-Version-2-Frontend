import { TrainingCordinator } from './training-cordinator';

describe('TrainingCordinator', () => {
  it('should create an instance', () => {
    expect(new TrainingCordinator()).toBeTruthy();
  });
});
